import type { FC, ReactNode } from "react";
import { Fragment } from "react";

/**
 * The ScrollOverlay is a
 */
export const ScrollOverlay: FC<{ children: ReactNode; pages: number }> = _props => {
  /* const {
   *   gl: { domElement },
   * } = useThree(); */

  return <Fragment></Fragment>;
};
